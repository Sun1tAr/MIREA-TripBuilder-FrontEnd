# Travel Planner - HTML/CSS Прототип

Полнофункциональный HTML/CSS прототип веб-приложения "Планировщик путешествий" с адаптивным дизайном для мобильных, планшетов и десктопных устройств.

---

## 🚀 Быстрый старт

### 1. Развёртывание локального сервера

#### Вариант 1: Python (встроен в большинство систем)

```bash
# Python 3.x
python -m http.server 8000

# Python 2.x
python -m SimpleHTTPServer 8000
```

Откройте в браузере: **http://localhost:8000**

#### Вариант 2: Node.js

```bash
# Установите http-server (если не установлен)
npm install -g http-server

# Запустите сервер
http-server

# Откройте http://localhost:8080
```

#### Вариант 3: Live Server в VS Code

1. Установите расширение "Live Server" (автор: Ritwick Dey)
2. Правый клик на `index.html` → "Open with Live Server"
3. Автоматически откроется в браузере на `http://localhost:5500`

#### Вариант 4: npx serve

```bash
npx serve

# Откройте http://localhost:3000
```

---

## 📁 Структура файлов

```
travel-planner-prototype/
│
├── index.html                 # Главная страница (Лента идей)
├── route-builder.html         # Конструктор маршрута
├── checklist.html             # Список дел
├── saved-trips.html           # Сохранённые путешествия
├── trip-details.html          # Детали путешествия
├── 404.html                   # Страница ошибки
│
├── css/
│   ├── variables.css          # CSS переменные (цвета, типография, размеры)
│   ├── base.css               # Base стили и типография
│   ├── components.css         # Переиспользуемые компоненты (кнопки, карточки, модали)
│   ├── layout.css             # Структура (header, sidebar, footer, grid)
│   └── responsive.css         # Медиа-запросы для адаптивности
│
├── js/
│   └── navigation.js          # Интерактивность (меню, модали, формы)
│
├── assets/
│   ├── icons/                 # SVG иконки
│   └── images/                # Изображения (placeholder)
│
└── README.md                  # Этот файл
```

---

## 🎨 Функциональность

### Страницы (5 функционально разных)

| Страница | Функционал | URL |
|----------|-----------|-----|
| **index.html** | Лента идей путешествий, фильтры, лайки | `/` |
| **route-builder.html** | Конструктор маршрутов, добавление точек | `/route-builder.html` |
| **checklist.html** | Списки дел с приоритетами | `/checklist.html` |
| **saved-trips.html** | Каталог путешествий, поиск | `/saved-trips.html` |
| **trip-details.html** | Детали путешествия, табы, совместный доступ | `/trip-details.html` |
| **404.html** | Страница ошибки | `/404.html` |

### Компоненты

✅ **Header** - Логотип, поиск, уведомления, профиль, бургер-меню  
✅ **Sidebar** - Навигация, ссылки страниц, информация пользователя  
✅ **Footer** - Информация о компании, ссылки, соцсети  
✅ **Кнопки** - Основные, вторичные, outline, размеры (sm, lg), значки  
✅ **Карточки** - Card, Card Header, Card Body, Card Footer, Image  
✅ **Формы** - Input, Textarea, Select, Groups, Валидация  
✅ **Модали** - Popup окна, закрытие, анимации  
✅ **Табы** - Переключение между содержимым  
✅ **Списки** - Таблицы, чеклисты с приоритетами  
✅ **Сетка** - Grid для карточек (адаптивный)  
✅ **Значки** - Badge, Label, Status  

---

## 📱 Адаптивность (3 класса размеров)

### Breakpoints

```css
Mobile:   0px — 640px        (смартфоны)
Tablet:   641px — 1024px     (планшеты)
Desktop:  1025px+            (мониторы)
```

### Изменения по размеру

| Элемент | Mobile | Tablet | Desktop |
|---------|--------|--------|---------|
| Sidebar | Drawer (скрыта) | Узкая видима | Полная видима |
| Header | Компактный | Нормальный | Нормальный |
| Grid | 1 колонна | 2 колонны | 3 колонны |
| Padding | 12px | 16px | 20px |
| Шрифты | Меньше | Средние | Нормальные |

---

## 🎨 Цветовая схема

### Основные цвета

```css
--color-primary: #2196F3;        /* Синий */
--color-accent: #FF6F00;         /* Оранжевый */
--color-success: #4CAF50;        /* Зелёный */
--color-warning: #FFC107;        /* Жёлтый */
--color-error: #F44336;          /* Красный */
```

### Нейтральные

```css
--color-bg-primary: #FAFAFA;     /* Светлый фон */
--color-bg-secondary: #FFFFFF;   /* Белый */
--color-text-primary: #212121;   /* Чёрный текст */
--color-text-secondary: #757575; /* Серый текст */
--color-border: #E0E0E0;         /* Границы */
```

### Использование

Все цвета находятся в `css/variables.css`. Для кастомизации отредактируйте CSS переменные `:root`.

---

## 🔧 Интерактивные функции

### JavaScript функции (в `js/navigation.js`)

✅ Hamburger меню (мобайл)  
✅ Active nav item (текущая страница)  
✅ Модали (открытие/закрытие)  
✅ Форм-валидация  
✅ Checkbox toggle  
✅ Like button  
✅ Search функционал  
✅ Копирование в буфер обмена  
✅ Экспорт таблиц в CSV  
✅ Notifications  

---

## 🎯 CSS Методология

### BEM Naming

```css
.component { }              /* Блок */
.component__element { }     /* Элемент */
.component--modifier { }    /* Модификатор */

/* Пример */
.card { }
.card__header { }
.card__body { }
.card--active { }
```

### Утилиты

```html
<div class="flex items-center gap-16">
    <button class="btn btn--primary btn--lg">Button</button>
    <div class="card">Content</div>
</div>
```

---

## 🚀 Переход в React

### Шаг 1: Экспортировать компоненты

Каждый компонент HTML можно конвертировать в JSX:

```html
<!-- HTML -->
<button class="btn btn--primary">Сохранить</button>
```

```jsx
// React
<Button variant="primary">Сохранить</Button>
```

### Шаг 2: Копировать CSS

CSS файлы можно переиспользовать в React:

```
HTML CSS → React CSS Modules / Tailwind / Styled Components
```

### Шаг 3: Добавить логику

```jsx
const [items, setItems] = useState([]);
const [isLoading, setIsLoading] = useState(false);

// Fetch от API
useEffect(() => {
  fetchTrips().then(data => setItems(data));
}, []);
```

---

## 📋 Требования к учебному проекту

✅ **1. HTML + CSS + JavaScript** — Используется  
✅ **2. 5 функционально разных страниц** — index, route-builder, checklist, saved-trips, trip-details  
✅ **3. Адаптивный дизайн (3 breakpoints)** — Mobile, Tablet, Desktop  
✅ **4. Header, Sidebar, Footer** — На каждой странице  
✅ **5. Локальный сервер** — Используйте Python/Node.js  
✅ **6. Mock-данные** — Hardcoded в HTML  
✅ **7. Маршрутизация** — Между страницами через ссылки  
✅ **8. Обработка 404** — 404.html страница готова  

---

## 📸 Демо функционала

### Интерактивные элементы

- 🔔 Нажмите на иконку уведомлений в header
- 📱 Откройте на мобайле — откроется drawer меню
- ❤️ Нажмите на like button на идеях
- ✓ Отмечайте дела в чеклисте
- 🔍 Используйте поиск для фильтрации
- 📤 Нажмите "Поделиться" на странице деталей путешествия

---

## 🛠️ Кастомизация

### Изменить цвета

Отредактируйте `css/variables.css`:

```css
:root {
  --color-primary: #YOUR_COLOR;
  --color-accent: #YOUR_COLOR;
}
```

### Изменить шрифты

```css
:root {
  --font-family-base: 'Your Font', sans-serif;
  --font-size-base: 16px;
}
```

### Добавить новую страницу

1. Создайте `new-page.html`
2. Скопируйте структуру из `index.html`
3. Добавьте ссылку в sidebar навигацию
4. Используйте существующие компоненты

---

## 🐛 Debugging

### Консоль браузера (F12)

```javascript
// Проверить CSS переменные
getComputedStyle(document.documentElement).getPropertyValue('--color-primary')

// Проверить элементы
document.querySelectorAll('.card')

// Логирование
console.log('My data:', data);
```

### DevTools Mobile Emulation

1. Нажмите F12
2. Нажмите Ctrl+Shift+M (Windows) или Cmd+Shift+M (Mac)
3. Выберите устройство из выпадающего меню

---

## 📚 Ресурсы

- [MDN Web Docs](https://developer.mozilla.org/)
- [CSS Grid Guide](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Grid_Layout)
- [Flexbox Guide](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Flexible_Box_Layout)
- [Responsive Design](https://developer.mozilla.org/en-US/docs/Learn/CSS/CSS_layout/Responsive_Design)

---

## ✅ Чек-лист перед сдачей

- [ ] Все 6 страниц работают
- [ ] Адаптивность работает на 3 breakpoints
- [ ] Header + Sidebar + Footer на каждой странице
- [ ] Mock-данные загружаются
- [ ] Навигация между страницами работает
- [ ] 404 страница доступна
- [ ] Локальный сервер запускается без ошибок
- [ ] Все стили применены правильно
- [ ] Модали открываются/закрываются
- [ ] Форм-валидация работает

---

## 📞 Support

При возникновении проблем:

1. **Проверь консоль**: F12 → Console
2. **Проверь пути**: Убедись, что пути к CSS/JS/Images правильные
3. **Очисти кеш**: Ctrl+Shift+Delete или Cmd+Shift+Delete
4. **Перезагрузи сервер**: Заверши процесс и запусти снова

---

**Готово! Используй этот прототип как основу для React разработки. Удачи! 🚀✈️**
