server {
    listen 80;
    server_name localhost;

    # Nginx сразу смотрит в /usr/share/nginx/html
    # То есть в /dist нашего приложения
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
